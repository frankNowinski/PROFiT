%nav.navbar.navbar-toggleable-md.navbar-light.bg-faded.navbar-inverse.bg-inverse
  .container
    = link_to 'Show Me the Money', home_path, { class: 'navbar-brand' }

    - if user_signed_in?
      = render 'navbar/signed_in_nav'

